@component('components/layout', { title: 'Résultats' })
  <div class="container mt-3 text-center">
    <h3>
      Vous avez terminé le deck: {{ deck.name }}
    </h3>
    <p>
      Résultat {{ goodAnswers }} / {{ deck.cards.length }}
    </p>
    <form action="{{ route('home') }}">
      <button type="submit" class="btn btn--primary" value="submit">OK</button>
    </form>
  </div>
@end
