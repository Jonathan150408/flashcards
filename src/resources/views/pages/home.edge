@component('components/layout', { title: 'Accueil' })
  <h3>
    Mes decks
  </h3>
  <section>
    {{--  Lister les decks  --}}
    @each((deck, index) in decks)
      <div class="deck">
        <h4>
          {{ deck.name }}
        </h4>
        <p>
          {{ deck.description }}
        </p>
        <script>
          console.log(cardsCounts);
        </script>
        <p>
          {{ deck.cards.length }} cartes
        </p>
      </div>
    @end
    <a href="{{ route('decks_create') }}">+</a>
  </section>

  <style>
    h3 {
      text-align: center;
      margin: 20px 0;
    }
    
    section {
      display: flex;
      gap: 20px;
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
    }
    
    .deck,
    section a {
      border: 2px solid black;
      border-radius: 20px;
      padding: 10px;
      width: 200px;
      height: 200px;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 10px;
    }
    
    section a {
      color: black;
      background-color: lightblue;
      text-decoration: none;
      font-size: 5em;
    }
  </style>
@endcomponent
