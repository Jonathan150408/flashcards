@component('components/layout', { title: 'Accueil' })
  <h3>
    Liste des decks
  </h3>
  @each((deck, index) in decks)
    <div>
      <h4>
        {{ deck.name }}
      </h4>
      <p>
        {{ deck.description }}
      </p>
    </div>
  @end
@endcomponent
